"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .issuingintervallimit import IssuingIntervalLimit
from datetime import datetime
from moovio_sdk.models import components
from moovio_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class IssuedVelocityLimitTypedDict(TypedDict):
    r"""A velocity limit with its current runtime state."""

    interval: IssuingIntervalLimit
    r"""Specifies the time frame for a velocity limit. `per-transaction` applies to each individual authorization and never resets. Time-based intervals (where supported) reset at midnight ET."""
    amount: NotRequired[int]
    r"""The maximum amount in cents that can be spent in a given interval."""
    count: NotRequired[int]
    r"""The maximum number of transactions allowed in the given interval."""
    amount_used: NotRequired[int]
    r"""The amount in cents already spent in the current interval."""
    amount_remaining: NotRequired[int]
    r"""The amount in cents remaining in the current interval."""
    count_used: NotRequired[int]
    r"""The number of transactions already made in the current interval."""
    count_remaining: NotRequired[int]
    r"""The number of transactions remaining in the current interval."""
    resets_on: NotRequired[datetime]
    r"""When the current interval resets. Absent for per-transaction limits."""


class IssuedVelocityLimit(BaseModel):
    r"""A velocity limit with its current runtime state."""

    interval: IssuingIntervalLimit
    r"""Specifies the time frame for a velocity limit. `per-transaction` applies to each individual authorization and never resets. Time-based intervals (where supported) reset at midnight ET."""

    amount: Optional[int] = None
    r"""The maximum amount in cents that can be spent in a given interval."""

    count: Optional[int] = None
    r"""The maximum number of transactions allowed in the given interval."""

    amount_used: Annotated[Optional[int], pydantic.Field(alias="amountUsed")] = None
    r"""The amount in cents already spent in the current interval."""

    amount_remaining: Annotated[
        Optional[int], pydantic.Field(alias="amountRemaining")
    ] = None
    r"""The amount in cents remaining in the current interval."""

    count_used: Annotated[Optional[int], pydantic.Field(alias="countUsed")] = None
    r"""The number of transactions already made in the current interval."""

    count_remaining: Annotated[
        Optional[int], pydantic.Field(alias="countRemaining")
    ] = None
    r"""The number of transactions remaining in the current interval."""

    resets_on: Annotated[Optional[datetime], pydantic.Field(alias="resetsOn")] = None
    r"""When the current interval resets. Absent for per-transaction limits."""

    @field_serializer("interval")
    def serialize_interval(self, value):
        if isinstance(value, str):
            try:
                return components.IssuingIntervalLimit(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "amount",
                "count",
                "amountUsed",
                "amountRemaining",
                "countUsed",
                "countRemaining",
                "resetsOn",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    IssuedVelocityLimit.model_rebuild()
except NameError:
    pass
