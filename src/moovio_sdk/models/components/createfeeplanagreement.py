"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from moovio_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class CreateFeePlanAgreementTypedDict(TypedDict):
    plan_id: str
    r"""A unique identifier for a fee plan: the pricing terms, such as flat-rate or cost-plus, that an account
    is charged under its fee plan agreement. Use GET /accounts/{accountID}/fee-plans to list the fee plans
    available to assign to a given account.
    """
    prior_agreement_id: NotRequired[str]
    r"""The account's active fee plan agreement to supersede. When set, that agreement is terminated and
    the new one takes its place in a single operation, so the account is never without an active fee
    plan agreement. This new agreement always receives a newly issued agreementID.

    Omit it if the account doesn't already have an active fee plan agreement.
    """


class CreateFeePlanAgreement(BaseModel):
    plan_id: Annotated[str, pydantic.Field(alias="planID")]
    r"""A unique identifier for a fee plan: the pricing terms, such as flat-rate or cost-plus, that an account
    is charged under its fee plan agreement. Use GET /accounts/{accountID}/fee-plans to list the fee plans
    available to assign to a given account.
    """

    prior_agreement_id: Annotated[
        Optional[str], pydantic.Field(alias="priorAgreementID")
    ] = None
    r"""The account's active fee plan agreement to supersede. When set, that agreement is terminated and
    the new one takes its place in a single operation, so the account is never without an active fee
    plan agreement. This new agreement always receives a newly issued agreementID.

    Omit it if the account doesn't already have an active fee plan agreement.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["priorAgreementID"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    CreateFeePlanAgreement.model_rebuild()
except NameError:
    pass
